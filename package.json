{
  "name": "sentinel-landslide-system",
  "version": "1.0.0",
  "description": "SENTINEL Citizen & State Emergency Operations Center Disaster Warning System",
  "main": "frontend/citizen.html",
  "scripts": {
    "build:citizen": "node scripts/build-citizen.js",
    "cap:add": "cap add android",
    "cap:sync": "npm run build:citizen && cap sync android",
    "cap:build": "node scripts/package-apk.js",
    "build:apk": "node scripts/package-apk.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  }
}
